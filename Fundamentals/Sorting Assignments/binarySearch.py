"""You have been given a sorted(in ascending order)integer array/list(ARR) of size N and an element X.Write a function to search this element in the giveninput array/list using 'Binary Search'. Return the index of theelement in the input array/list. In case the element is not presentin the array/list, then return -1."""def binarySearch(arr, n, x):    start = 0    end = n - 1    while start <= end:        mid = (start + end) // 2        if arr[mid] > x:            end = mid - 1        elif arr[mid] < x:            start = mid + 1        else:            return mid    return -1# Driver Coden = int(input("Enter N : "))print("Enter elements in array : ")arr = [int(x) for x in input().split()]arr.sort()print("Sorted array : ", arr)x = int(input("Enter X : "))res = binarySearch(arr, n, x)print(x, " is at ", res+1)